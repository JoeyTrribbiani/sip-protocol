# 安全策略

## 支持版本

| 版本 | 安全维护状态 |
|------|-------------|
| 2.1.x | ✅ 支持 |
| ≤ 2.0.x | ❌ 不再维护（v2.0 起为 encryption-only 架构，旧版含已移除的明文落盘 file_transfer） |

## 攻击面与信任模型

SIP（Secure Inter-agent Protocol）是 Agent 间端到端加密通道库。完整的威胁模型、
防线分层与已知限制见 [docs/SPEC.md §12 安全考虑](./docs/SPEC.md)。摘要：

- **在范围内**：传输通道被完全控制下的机密性与完整性（AEAD）、中间人（PSK + 三重 DH）、
  重放（replay_tag + 单调计数器 + 时间戳窗）、密钥轮换前向保密（rekey 闭环）、
  加密文件工件的篡改/重排/拼接/截断（AEAD tag 链）
- **明确不在范围**：流量分析与元数据保护、后量子安全（设计稿见
  `docs/superpowers/specs/2026-04-22-post-quantum-kex-design.md`）、密钥托管、
  传输层拒绝服务、Python 运行时以下的内存取证
- **关键设计取舍（已在规范登记）**：AEAD 实为 ChaCha20-Poly1305（12 字节随机 nonce，
  非 XChaCha20）；Argon2id 固定盐（域常量语义）；Python 运行时下旧密钥擦除为尽力而为
  ——见 [SPEC §13 实现偏差登记](./docs/SPEC.md)

## 报告漏洞

**请勿通过公开 Issue / PR / 讨论区报告安全漏洞。**

1. 使用 GitHub 私有安全报告入口（Issue 页面 "Report a vulnerability"，本文件启用该入口）
2. 或联系维护者邮箱（见仓库 owner 主页）

期望的响应时限：

| 阶段 | 目标 |
|------|------|
| 确认收到 | 3 个工作日 |
| 初步评估（是否接受/严重级别） | 7 个工作日 |
| 修复与披露协调 | 视严重级别，critical 目标 30 天内 |

报告内容建议：影响版本、复现步骤或 PoC、攻击前提（是否需要 PSK/私钥）、
受影响的 SPEC 章节（便于快速定位）。

## 披露政策（Draft）

- 修复发布前不公开细节；修复随 CHANGELOG 记录安全条目
- 报告者在修复发布后可公开披露（我们请求预留 90 天协调窗口，可协商）
- 安全修复经 `pip-audit` CI 层与测试回归（283 用例）验证后合入
- 已知但登记在案的架构限制（SPEC §13）不视为漏洞，欢迎对登记项的加固建议
